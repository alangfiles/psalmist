import React from "react";
import ReactDOM from "react-dom/client";
import PsalmistApp from "./Psalmist";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PsalmistApp />
  </React.StrictMode>
);
